# UD code-gate + multi-turn rerun with client-inclusive accounting — 2026-09-14

Result: **PASS** — formal repeat of the UD-Q4_K_XL Hermes code gate, now with
multi-turn artifact reuse, a hard <=1800 s work deadline, and kernel-accounted
client RAM. Run dir: `run-20260914T104344-e2e` (e2e-summary.json, summary.json,
inclusive-ram.jsonl, client-final.json, hermes/codegate transcripts).

## What changed since the 2026-09-12 formal PASS

Commits 9f3c8152 + 71bc47f5 (this slice, TDD: 20 CPU tests in
tests/test-e2e-client-scope-20260914.py, all green; the 5 pre-existing E2E test
files and the harness suites stay green):

1. **Deadline cap**: E2E_WORK_SECONDS is hard-capped at 1800 s (Seppe steering
   2026-09-13 08:45). The retired 2700 s code default now fails closed at
   import; the physical run used WORK_SECONDS=1800.
2. **Client-inclusive cgroup accounting**: the runner (and with it the hermes
   client, artifact probe and curl) executes inside a dedicated systemd scope
   with MemoryMax=4G (`run-e2e-guarded.sh`, outer shell outside both scopes).
   Every guard sample records server+client kernel peaks to
   `inclusive-ram.jsonl` and refuses a combined footprint above 40 GiB.
   Pattern adopted from the proven 2026-09-14 nightly UD run
   (.hermes-work/daily-20260914/run-ud.py). Raw launches without
   E2E_CLIENT_UNIT are refused before any host change.
3. **Honest metadata**: the summary records the actual checkpoint
   (E2E_CHECKPOINT, validated against the harness alias map), the actual task
   text/kind, the work budget and the client unit — previously a hardcoded
   ps-iq2xxs/marker pair. A follow-up fix (71bc47f5) mirrors a completed E2E
   verdict into the harness summary status; NOTE: this fix landed AFTER the
   physical run below, so in `run-20260914T104344-e2e/summary.json` the top-level
   `status` still reads the misleading default `blocked_or_failed` — the
   authoritative verdict is `e2e.status: completed` (embedded) and
   `e2e-summary.json`. Future runs are honest by construction.
4. UD now defaults to NCMOE 40 (the dev-serving default; A/B checkpoints keep 36).

No delegate review this run: the profile-level gpt-6-astra pause mandate runs
until ~2026-09-15 (higher-priority instruction, reconfirmed in the 2026-09-14
night-run state). Compensation: self-verification with file:line checks and a
real physical execution; no independent approval claimed. The delegation pin
is unchanged.

## Physical run (run-20260914T104344-e2e)

Configuration: UD-Q4_K_XL 4 shards, NCMOE 40, CTX 65536, KV q8_0, MTP shared
drafter n_max=4, reasoning off (the dev default the gate mirrors), isolated
HERMES_HOME, E2E_TASK=code, E2E_MULTI_TURN=1.

- Tier A guard at model readiness: GPU 4% util, 1751 MiB VRAM, MemAvailable
  44.6 GiB; launcher DRY guard OK; GGML_CUDA_NO_PINNED=1.
- Turn 1 (code task): hermes rc 0, wall 799.5 s; marker `code-gate-ok-7391`
  present after the query echo; shell-tool evidence in transcript; independent
  artifact probe `python3 /tmp/e2e-codegate.py` rc 0 with EXACTLY
  `code-gate-ok-7391`. The artifact (written by the agent) uses the required
  for-loop over range(3).
- Turn 2 (resume + reuse): rc 0, wall 10.6 s; resumed session
  20260914_104501_5d87e6, re-ran the EXISTING script without rewriting
  (0.0 s terminal call), answered exactly `code-gate-ok-7391`.
- Stale-artifact guard removed both /tmp gate files before the client started.
- Total work: started 10:43:44, finished 10:58:32 (~888 s), inside the
  enforced 1800 s deadline.

Budgets (both caps held, kernel-accounted on BOTH sides):

- Server scope: memory.peak 32.406467 GiB / 36G MemoryMax; client scope:
  memory.peak 0.374973 GiB / 4G MemoryMax; combined kernel-peak upper bound
  32.781441 GiB / 40 GiB budget. The sum of two kernel peaks is a CONSERVATIVE
  UPPER BOUND (peaks need not be simultaneous); it is not an exact concurrent
  measurement. memory.events max/oom/oom_kill/oom_group_kill = 0 in both
  scopes; client swap 0 (MemorySwapMax=0).
- VRAM sampled peak 24.052 GiB / 28 GiB; 844 samples, max gap 2.04 s — the
  2026-09-12 sampling-gap class is closed (guards are sampled DURING both
  agent turns and the probe), but sampling is still not a continuous-peak
  guarantee.
- Server swap: reported separately in samples.jsonl (memory.swap.current);
  swap stays outside the 40/28 budgets by project convention.

Request timings (server.log, reasoning-OFF agent workload, MTP acceptance
1.00000 on the five logged blocks): five logged completions; the dominant
cost was one 14213-token cold prefill at 18.84 tok/s (754 s) plus 83-token
decode at 8.28 tok/s; the other decodes were 5.56–18.29 tok/s on 10–35-token
outputs. These are agent-workload numbers at 64K context with reasoning off —
NOT the 2x256 throughput benchmark (UD reasoning-auto: 13.02 first / 27.94
second tok/s on 2026-09-14 night run) and NOT evidence for or against the
>=20 tok/s goal. Request-level attribution from timestamps is heuristic.

## Host lifecycle

Jetski was in `failed` state before this run (deliberate SIGINT stop at
08:37:31, not a crash). The harness stopped it (no-op) before GPU work and its
restore step STARTED it afterwards — end state verified active, consistent
with the standing daytime mandate. headless-chromium restored active via
gpu-host-prep.sh --restore. qli.service: skipped as masked/not-found per the
pause mandate and additionally guarded in code (E2ERun refuses any qli
mutation); never started. cleanup_errors []; scope stop verified; no
llama-server left; no listeners on 8102 after teardown.
